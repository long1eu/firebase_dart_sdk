name: cloud_firestore_dart_example
description: Example app for cloud_firestore_dart
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3

  firebase_core: ^0.4.4+3
  firebase_core_dart: ^0.0.1
  firebase_auth: ^0.16.0
  firebase_auth_dart: ^0.0.1
  cloud_firestore: ^0.13.5
  cloud_firestore_dart:
    path: ../

flutter:
  uses-material-design: true
